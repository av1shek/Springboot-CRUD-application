apiVersion: v1
kind: Secret            # Specifies the type of resource to be created (secret)
metadata:
  name: mysql
  namespace: mynamespace
data:                  # Specifies the data to be stored in the secret
  mysql.password: cGFzc3dvcmQ=   # base64 encoded value of "password"
  mysql.user: cm9vdA==          # base64 encoded value of "root"
type: Opaque           # Specifies the type of secret (opaque)
